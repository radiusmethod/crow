# Changelog

All notable changes to Crow will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- CROW-1220 — **Coder registers the PR on the Crow session, and sandboxed `crow` can reach the daemon.** After `gh pr create`, the first-prompt template (`skills/crow-workspace/SKILL.md`) runs `gh pr view --json url,number` then `crow add-link --session $CROW_SESSION_ID --type pr`. `add-link --type pr` is idempotent (skips if a `.pr` already exists or that URL is already linked — automation uses the first `.pr`, #946). `setup.sh` always writes `CROW_SESSION_ID` into the worktree `.claude/settings.local.json` `env`. When `crow.sock` is unreachable, the CLI retries loopback `POST /rpc` (same Origin + `localOnlyDenial` gates as the WebSocket) so Cursor's sandbox does not need to be disabled; `CROW_SOCKET` still pins the Unix path unless `CROW_HTTP_URL`/`CROW_HTTP_PORT` is set.

- CROW-1210 — **Auto-download the corveil CLI** from the public [`corveil/corveil-releases`](https://github.com/corveil/corveil-releases) repo. Opt in with `crow defaults set --corveil-auto-update true` (or Settings → General); pin with `--corveil-version vX.Y.Z` or leave `latest`. Crow checksum-verifies the host-platform asset, stores it under `~/Library/Application Support/crow/bin/corveil/<tag>/`, hot-swaps `.claude/bin/corveil`, reinstalls skills, and keeps the previous version for rollback. An operator-set source-build path is never overwritten. Offline / missing-asset / checksum-mismatch keep last-good and never break startup. Default **off**. Decision: [ADR 0025](docs/adr/0025-auto-download-corveil-cli.md).

- CROW-1209 — **Muse Jira MCP bridge.** `MuseMCPConfigWriter` mirrors the user's `jira` server from `~/.claude.json` into `~/.config/muse/settings.json` (`mcp_servers.jira`) on Muse launch (not daemon boot — same reason as Cursor #829 / Grok CROW-1205 / Antigravity CROW-1207: do not copy the token onto a box that merely has `muse` on PATH). Schema confirmed against official Muse docs (2026-09-08): `transport` is `stdio` or `streamable_http`; Crow writes `mode: "optional"` so a down Jira MCP does not abort the run; creating/rewriting the file injects `"schema_version": 1` (omitting it bricks every `muse` command). Merge-preserving: a user-authored entry is left alone; a `0600` sidecar lets Crow refresh or un-mirror its own write. `MuseLauncher` instructs `jira_*` when a Claude source is present and keeps `acli` only as the no-MCP fallback. Review clones still strip `.muse/` + `.agents/` (user-scope MCP is not a project file).

- CROW-1207 — **Antigravity Jira MCP bridge.** `AntigravityMCPConfigWriter` mirrors the user's `jira` server from `~/.claude.json` into `<$GEMINI_CONFIG_HOME or ~/.gemini/config>/mcp_config.json` (`mcpServers.jira`) on Antigravity launch (not daemon boot — same reason as Cursor #829 / Grok CROW-1205: do not copy the token onto a box that merely has `agy` on PATH). Merge-preserving: a user-authored entry is left alone; a `0600` sidecar lets Crow refresh or un-mirror its own write. Remote Claude `url` becomes Antigravity's `serverUrl`. `AntigravityLauncher` instructs `jira_*` when a Claude source is present and keeps `acli` only as the no-MCP fallback. Review clones still strip `.agents/` + `.gemini/`.

- CROW-1205 — **Grok Jira MCP bridge.** `GrokMCPConfigWriter` mirrors the user's `jira` server from `~/.claude.json` into `<$GROK_HOME or ~/.grok>/config.toml` (`[mcp_servers.jira]`) on Grok launch (not daemon boot — same reason as Cursor #829: do not copy the token onto a box that merely has `grok` on PATH). Merge-preserving: a user-authored table is left alone; a `0600` sidecar lets Crow refresh or un-mirror its own write. `GrokLauncher` instructs `jira_*` when a Claude source is present and keeps `acli` only as the no-MCP fallback. Review clones still strip `.grok/config.toml`.

- CROW-1201 — **systemd `--user` autostart on Linux.** `crow autostart` and Settings → General → Autostart were platform-neutral already; macOS had a launchd backend and Linux returned `supported: false` with a leftover pointer at closed #645. `SystemdAutostart` writes `~/.config/systemd/user/com.corveil.crowd.service`, `daemon-reload`s, and `enable`s (and `start`s only when nothing is listening on the target socket). `Restart=on-abnormal` mirrors launchd's `KeepAlive: {Crashed: true}` so a duplicate-instance `exit(1)` is not respawned. Logs go to `$XDG_STATE_HOME/crow/crowd.log` (fallback `~/.local/state/crow/crowd.log`) so CrowLog's file rotation still applies.

- CROW-1199 — **Opt-in unsigned macOS release path.** Repository/org variable `CROW_ALLOW_UNSIGNED_RELEASE=true` lets a `v*` tag publish when Apple signing secrets are absent: `scripts/macos-sign-notarize.sh --skip-signing` re-packs the unsigned universal CLI tarball/zip and `Crow.app` zip to the usual artifact paths, the GitHub Release is always a **prerelease**, and the notes say the build is unsigned and Gatekeeper-quarantined (`xattr -dr com.apple.quarantine <path>` or right-click → Open). Default remains fail-closed (ADR 0021). `--skip-notarize` still signs. Flip the variable off once Developer ID secrets exist.

- CROW-1189 — **Crow.app with `crowd` as a Tauri sidecar.** `make app CONFIG=release` runs `tauri build` and writes `release-app/Crow.app` containing `crowd` (and `crow`) plus the SwiftPM `.bundle` resources the web UI and tmux backend need. Debug `make run` still uses `.build/debug/crowd` / `CROWD_BIN`. The window still reuses a listening crowd and only SIGTERMs a sidecar it spawned. `v*` releases attach a stapled `Crow-<version>-macos.app.zip` next to the CLI tarball; the CLI `crow` codesign id is now `com.corveil.crow.cli` so it does not collide with the `.app` (`com.corveil.crow`). `crow autostart` still execs the standalone signed `crowd`. Decision: [ADR 0024](docs/adr/0024-tauri-sidecar-app.md).

- CROW-1163 — **Escape returns to the session grid** when the full session was opened from a grid cell. A `‹ Grid` control appears in the session header (title teaches `Esc` unless the session switcher is bound to an Escape prefix such as `esc+tab`). The binding is capture-phase on `document` but **does not fire while the xterm has focus** — Claude Code and other agents use Escape to cancel, and `web/terminal.js` forwards keystrokes to the PTY, so a global binding would steal it. Overlays (Settings, lightbox, in-page prompts, the session switcher, context menus) keep first refusal. When the switcher binding uses Escape as a prefix, the handler yields so `Esc` then `Tab` still opens the switcher from chrome; use **‹ Grid** to go back. Sessions opened from the sidebar, a ticket, or a deep link are unchanged.

- CROW-1153 — **Session grid.** A new top-level board (`#/grid`) shows up to 16 sessions at once so a fleet can be watched without tabbing. Each cell is a read-only capture-pane snapshot of that session's managed terminal (not a live PTY — 16 attaches would SIGWINCH the shared tmux window; [ADR 0022](docs/adr/0022-session-grid-snapshots.md)). Pin from the session menu or the cell header; pins persist per browser and lead the roster, with active sessions auto-filling the rest. Overflow paginates rather than shrinking cells. Click a cell to open the full session. Web (and the Tauri shell that loads it) only.

- CROW-1149 — Ticket-board **Start Exploring**: a split-button next to **Start Working** bootstraps the same worktree / session / agent path with a read/explain-only seed prompt (no edits, no PR, no In Progress board move). A `crow:explore` label on the auto-create watcher (same toggle as `crow:auto`) dispatches that path; if both labels are present `crow:auto` wins and both are stripped. Explore is a tag on `.work` (`Session.isExplore`), not a new session kind. CLI siblings: `crow explore-issue` / `crow batch-explore-issues` (existing RPCs with `explore: true`). Worktrees are named `{repo}-{n}-explore-{slug}` so a later Start Working does not collide.

- CROW-1150 — **Signed + notarized macOS releases.** `v*` GitHub Releases of `crow` and `crowd` are signed with a Developer ID Application certificate (hardened runtime + timestamp) and notarized via `xcrun notarytool` using an App Store Connect API key — the same secret shape as radiusmethod/socketzero, without electron-builder. `scripts/macos-sign-notarize.sh` imports the `.p12` into an ephemeral keychain, signs every Mach-O in the release tree, re-packs the tarball, writes a zip (the notarized container; a zip cannot be stapled), and deletes the keychain in an `EXIT` trap so the cert never lands in the login keychain. Missing secrets fail the job rather than publishing an unsigned build. Local `make daemon` stays unsigned. `crowd` is signed standalone because launchd (`crow autostart install --binary`) execs it directly; there is no `.app` in this cut (ADR 0010 retired the SwiftUI app; the Tauri window does not yet bundle `crowd` as a sidecar). Default runner is GitHub-hosted `macos-15`; repository variable `CROW_SIGNING_RUNS_ON` retargets the job to `[self-hosted, macOS, signing]` once the native-host runner is provisioned through `corveil/corveil-ci`. Human prereqs and secret names: [docs/macos-release-signing.md](docs/macos-release-signing.md). Decision record: [ADR 0021](docs/adr/0021-signed-notarized-macos-releases.md).

- CROW-1126 — **Migrate manual Corveil gateways to the connection.** The first-class Corveil integration (epic #1117) makes `corveilConnection` the source of truth from which the AI gateway is derived, but installs that predate it still carry a hand-entered `x-citadel-api-key` gateway with no tie to an org, no rotation, and no revoke handle. Two local-only verbs bridge that gap. `crow corveil detect-gateways` scans the Manager and every workspace gateway for the `x-citadel-api-key` header and classifies each against the stored connection — `managed` (already equals the connection's derived gateway for a provisioned org), `linkable` (a plaintext key on the connection's base URL), or `manual` (with a `reason`: no connection, an `op://` value, or a base URL that doesn't match) — printing only a redacted key prefix, never the value. `crow corveil link-gateway (--workspace N | --manager) --org ID` then **adopts** a linkable gateway's existing plaintext key into the connection as that org's key: non-disruptive (the running key is unchanged) and offline, since the Corveil backend has no key→org lookup so the org is user-supplied. The adopted key is stored with an empty `keyID`, which is also the upgrade path — a later `corveil select-org` mints a real, revocable managed key in its place. `link-gateway` enforces the same precondition `detect` advertises as `linkable`: it refuses an `op://` value or a base URL that doesn't match the connection (trailing slashes ignored, like the rest of the Corveil stack), and it refuses to overwrite an org that already holds a **minted** key rather than silently dropping the handle `deselect-org` revokes by — retire that one with `deselect-org` (which revokes on Corveil) first, then link. Both verbs sit on the one local-only Corveil surface (ledger, lane, `localOnlyDenial`), and the AI-gateway docs, a new [ADR 0020](docs/adr/0020-first-class-corveil-integration.md), and the CLI reference are updated alongside.

### Changed

- CROW-1249 — **Scratch board items are distinct large cards.** Each row reuses `.board-card` chrome plus a dedicated `.scratch-row` treatment (padding, drop shadow, vertical gap) and a footer band for Explore / Ticket / Work / Done / Go to Session, so consecutive items no longer run together. Content and button behavior are unchanged. The CROW-1237 sidebar card class stays unused.

- CROW-1247 — **Adopt existing source-build corveil installs onto the auto-downloader.** When `defaults.corveilAutoUpdate` is on, Crow owns `binaries["corveil"]`: a previous `out/` path is downloaded, checksum-verified, and replaced with the managed CLI under Application Support (the old binary is not deleted). `crow defaults set --corveil-auto-update false` (or the Settings toggle) still leaves an operator path alone. Leftover `#1228` `corveilAutoUpdate: false` plus a non-managed path is one-shot onto the downloader and stamped with `corveilAutoUpdateOptOut` so a later explicit false is a real opt-out. Offline / checksum-mismatch keep last-good. Decision: [ADR 0025](docs/adr/0025-auto-download-corveil-cli.md).

- CROW-1242 — Split `boards.js` (~1.5k lines) into a board kernel plus focused classic scripts (`scorecard-board.js`, `tickets-board.js`, `reviews-board.js`, `scratch-board.js`). Shared widgets (`relTime`, `linkChip`, `labelPills`, spawn/split actions, filter inputs) and the CROW-771 refresh flags stay on the kernel; board *state* stays on `sidebar.js` (load-order / Tickets-card contract). `index.html` load order matches `StaticAssets.uiJavaScriptFiles`. No ES modules, bundler, hash-route, or board-behavior change — same pattern as CROW-1155 / CROW-1160 / CROW-1238.

- CROW-1238 — Split `sidebar.js` (~1.6k lines) into a state kernel plus focused classic scripts (`sidebar-chrome.js`, `pr-glyphs.js`, `session-row.js`, `session-menu.js`). Shared `let`/`const` (`sessions`, `GRID_*`, `ICONS`) stay on the kernel; `index.html` load order matches `StaticAssets.uiJavaScriptFiles`. No ES modules, bundler, hash-route, or sidebar-behavior change — same pattern as CROW-1155 / CROW-1160.

- CROW-1229 — **Default `defaults.corveilAutoUpdate` on.** A missing key decodes as on so a fresh Crow install downloads and links `corveil` from public `corveil/corveil-releases` without an operator toggle. An explicit `false` in `config.json` (Settings / `crow defaults set --corveil-auto-update false`) stays off. An operator-set source-build `binaries["corveil"]` is still skipped (`shouldAutoManage` / `isManagedPath`). Decision: [ADR 0025](docs/adr/0025-auto-download-corveil-cli.md).

- CROW-1222 — Split `TmuxBackend.swift` (~1.6k lines) into a thin facade plus focused `extension TmuxBackend` files (`TmuxServerLifecycle`, `TmuxWindowLifecycle`, `TmuxInput`, `TmuxScrollbackHealth`, `TmuxCockpitReaper`, `TmuxReadinessWatch`, `TmuxManagerExitMonitor`). Call sites keep `TmuxBackend.shared.*`; paste-Enter settle, alt-buffer latch, and the 50k history floor are unchanged.

- CROW-1215 — Claude Code **≥ 2.1.259** `--permission-prompts none` (deny anything that would prompt while auto still classifies; Containment Escape still applies) was evaluated against ADR 0015 and **declined**. It is print-mode only (`claude -p`; CLI reference: "Set who answers permission prompts in print mode"). Crow launches the interactive TUI in a tmux PTY, so the flag cannot convert CROW-1176's extra-workdir auto-mode Read stalls into denials for `JobScheduler` / Manager. Even if it became TUI-legal, applying it on the Manager would deny `$HOME` / `$TMPDIR` Reads that today only stall. Crow still emits `--permission-mode auto` and nothing else; launch tests pin that no Claude path emits `--permission-prompts`. [ADR 0015](docs/adr/0015-harness-capability-tiers.md) records the eval. Re-check only if the flag becomes TUI-legal — then `.job` only, never Manager.

- CROW-1176 — Claude `--permission-mode auto` is **no longer stall-free**. Claude Code ≥ 2.1.257 prompts once before the first file Read outside the working directories (launch cwd + `--add-dir` + `permissions.additionalDirectories`). Crow still emits `--permission-mode auto` for Manager / `.job` / (when enabled) coder-view Auto and **does not** paper over that prompt: `--dangerously-skip-permissions` / `bypassPermissions` would also drop the same-release Containment Escape rule (brittle-reject), `permissions.blockReadsOutsideWorkingDirectories` only *blocks* extra-workdir Reads, and a blanket `--add-dir` of `$HOME` / `$TMPDIR` / sibling checkouts is the too-permissive fake. Review clones under `{devRoot}/crow-reviews/` are the likely first hit. Matrix Auto-permission cell is now ⚠️; [ADR 0015](docs/adr/0015-harness-capability-tiers.md) records the eval. A live Crow-tmux PTY pass on ≥ 2.1.257 remains a human re-check (eval binary was 2.1.232).

- CROW-1175 — Cursor CLI **2026.08.26** `agent persist` (keep running after disconnect; `/detach`, `attach`, `list`/`stop`, `--resume`) was evaluated against ADR 0015 and **declined**. Persist is Cursor's own tmux wrapper (`tmux -L cursor-agent`), not a missing `--continue` and not native RC. If `$TMUX` is already set it forwards argv to a normal `agent` launch — Crow always launches inside tmux — so persist as a Crow launch command would never persist. After `/detach` there is no TUI for `crow send` to paste into; `persist list`/`stop` are machine-global on that named server. Crow's tmux pane already *is* the survive-disconnect layer; `crow recreate-terminal` keeps `--continue`. Probed on installed `cursor-agent 2026.08.31-4057e58`. Launch tests pin that no Cursor path emits `persist`.

- CROW-1001 — Codex sessions show the **remote-control badge**, because they were always remotely drivable and the badge was the only thing saying otherwise. `OpenAICodexAgent.supportsRemoteControl` was `false` on a recorded reason — "Codex doesn't do remote control", elaborated in the matrix as "Codex's TUI isn't stdin-drivable the way `crow send` fakes RC for the others" — that was wrong in both halves, and wrong about *Crow*, not about Codex. `crow send` never writes stdin: `TerminalRouter.send` → `TmuxBackend.sendText` is tmux `load-buffer` → `paste-buffer` → `send-keys Enter`, a paste into the pane, so "this TUI refuses stdin" would not have implied undrivable even if it were true. And Codex accepts the paste — verified end-to-end in a live pane on `codex-cli 0.141.0`: the payload lands in the composer verbatim and the trailing Enter submits it (a `/status` round-trip returned the session panel). Nothing gated on the flag either — `TerminalRouter.send` is agent-agnostic, and `supportsRemoteControl` feeds only `remoteControlActiveTerminals` and the `remote_control_available` payload — so this fixes a **false-negative badge**, not a missing drive path: anyone who ever typed into a Codex session from Crow's web UI was already exercising the capability the UI denied. Codex now sits on the same footing as Cursor/OpenCode/Grok/Antigravity, where `true` has always meant "*Crow* can drive this", not "the harness ships an RC protocol". **No launch text changes** — Codex has no `--rc` and `autoLaunchCommand` still ignores `remoteControlEnabled`; a test pins that across every `SessionKind` plus the Manager launcher, so a future flag can't be added on the assumption the badge implies one. **Native `codex remote-control` was evaluated in the same pass and declined**, which is the other half of the ticket: `remote-control start` refuses on the npm build Crow resolves (it demands the managed standalone install at `~/.codex/packages/standalone/current/codex`, obtained via a different installer); `codex app-server daemon` is a machine-global singleton bound to one fixed control socket with no port/instance flag, so Crow's N concurrent per-worktree sessions cannot be addressed individually — the Codex hooks `cwd`-collision defect relocated onto the drive path; and `--remote` attaches a *local* TUI to a *remote* app server, the reverse of what the badge claims, since Crow already **is** the remote surface (browser → `crowd` → tmux). Those were probed on the installed `0.141.0` while stable was `0.147.0`, so the pin is honest about which blockers age: the direction argument is architectural, the other two are version-specific and are exactly what `0.147.0`'s added `remote-control pair` subcommand might move. None of it gates the badge, which rests on the paste path. Two things fell out of the same probe: Codex's live pane reported `alternate_on=0`, so its `usesAlternateScreen = false` is now **verified** rather than inherited from the inline default, and 0.141.0 gates a fresh directory behind *two* prompts — folder trust (which `CodexTrustSeeder` handles) and a newer hook-trust prompt listing Crow's global `~/.codex/hooks.json` entries, which is untested against unattended auto-launch and is noted in the gap audit as the next probe if a Codex `.job` ever stalls at startup. **One behavioural side effect, and it is a fix:** `/rename` passthrough now actually reaches Codex worker sessions. `agentRenameTargets` routes a Manager to its command-bearing terminals but sends work/job/review renames through `remoteControlRenameTargets`, i.e. `remoteControlActiveTerminals` — which Codex terminals never entered while the flag was `false`. So renaming a Codex work session forwarded nothing, even though the matrix's rename row read ✅ and `sessionRenameSlashCommand` returned a real `/rename`. With the flag `true` and `remoteControlEnabled` on, it lands. The paste can still only reach agent-launched terminals (`launchAgent` is what populates that set), so no Shell tab receives a `/rename`.

- CROW-999 — Codex `PostToolUse` hooks fire **async** when the installed `codex` is new enough. Upstream honored `async: true` from **0.148.0**; through `0.147.0` the hook engine parsed the key and then **skipped the entry**, so an async hook wasn't slower, it was *absent* — and since Crow's session-state detection runs on those hooks, emitting one would have silently frozen the state card. That is why `CodexHookConfigWriter.asyncEvents` sat empty since v0.139.0, and why the fix is a gate rather than a flag flip. `CodexVersionProbe` runs `codex --version` once at boot (hard-bounded by the same 3s race `BinaryIdentityProbe` uses, so a hung binary can't stall startup), parses it, and only clears the gate at `>= 0.148.0`. **Every uncertain answer lands on sync** — no Codex registered, no binary resolved, a spawn failure, a timeout, an unreadable banner — because sync is correct on every build and costs only latency, while a wrong `async` costs the hook. The gate also **rejects pre-releases of the pin**: async landed at `0.148.0-alpha.9`, so `0.148.0-alpha.1` is a build that would still drop the hooks, and standard semver precedence (a pre-release sorts below its release) rejects the whole alpha series without a special case — it starts passing on its own when the stable ships. Only `PostToolUse` is async, mirroring Claude: `PreToolUse` stays synchronous so it is *accepted* by the daemon ahead of the `PermissionRequest` that follows it, and making it async would widen precisely the inversion that does **not** self-heal (#903 — a `PreToolUse` applied after `PermissionRequest` clears the permission badge while the agent is parked at a prompt, with no further event until the user answers). A downgrade is handled: each event's entry is rebuilt whole on every boot, so a machine that stops clearing the gate has the `async` key **removed** rather than stranded. Probed once at boot like agent availability, so upgrading `codex` under a running `crowd` needs a restart. Semver parsing moved into CrowCore as `AgentSemVer`, shared with OpenCode's `--auto` window probe — with whole-token matching, so a `--version` preamble naming a path like `…/node/v22.22.2/bin/codex` can't be misread as the agent's version. **Not yet verified empirically:** the async *timing* (state-card correctness for `.job`/`.work`, no double-count against the `notify` bridge) has no observed pass, because `0.148.0` has not shipped stable yet — on 2026-08-13 npm's `latest` is `0.147.0` and `0.148.0-alpha.14` is the newest build, which the gate rejects. So this ships **inert on every stable install** and begins emitting `async` on its own once the release lands; that first stable is where the timing pass belongs.

- CROW-1002 — The session switcher's default binding is **`cmd+/`**, and **`shift+tab` is now refused outright**. `esc+tab` (CROW-980) fixed the Shift+Tab collision by making Esc a prefix, but a two-key sequence is a poor fit for the thing you reach for while reading a terminal: it arms on a 1.5s timer, any intervening keystroke breaks it, and because a prefix chord holds nothing down it can't commit on release — the overlay stays up until Enter. `cmd+/` is an ordinary modifier chord, so it opens on press and commits on release of Cmd the way a macOS app switcher does, with no timer and nothing to arm. It is also inert in the terminal: macOS produces no key data for a Cmd chord in xterm, so capturing it leaks nothing to the PTY. The sequence machinery is untouched and `esc+tab` remains a legal binding — it merely stopped being the default, so an install that has it keeps it. On a keyboard with no Cmd, set `ctrl+/`. One cosmetic consequence: the hint line separator moved from `/` to `or`, since the default's cycle key *is* `/` and "`/ / ←→ to cycle`" reads as a typo.

- CROW-980 — The session switcher's default binding is **`esc+tab`** (tap Esc, then Tab), not `shift+tab`. Shift+Tab is already Claude Code's permission-mode cycle, and `switcher.captureInTerminal` defaults to **true**, so the old default ate that keystroke inside every focused terminal. Esc is not a modifier, so this is not a string swap: a leading `esc` is now parsed as a **prefix key**, and `parseSwitcherBinding` returns it separately from `shift/ctrl/alt/meta`. Without that, `esc+tab` would have parsed identically to bare `tab` — every unmodified Tab opening the overlay and, in the terminal, Tab-completion going dead. The prefix arms for 1.5s and is broken by any intervening keystroke (a bare modifier excepted, since a resting Shift shouldn't cancel the sequence), and **Esc itself is never swallowed** — it has to stay instant for interrupting an agent, so it reaches the terminal on the way through and the overlay opens on the Tab that follows. A prefix chord holds nothing down, so it can't commit on release the way `shift+tab` does: the overlay stays open, Tab/←→ cycle, **Enter** (or a click) switches, Esc cancels — which is the ticket's reason for choosing this chord. The hint line under the strip states whichever gesture the configured chord actually uses. Existing installs are untouched: the new default reaches only a config with no `switcher.binding`, and `crow ui set --switcher-binding shift+tab` restores the old one. **Superseded within this same unreleased block by CROW-1002**, which moves the default again (to `cmd+/`) and closes the two holes this entry's last sentence describes: leaving existing installs untouched is precisely what left them eating Shift+Tab, and restoring the old binding is no longer possible — it is reserved.

### Removed

- CROW-1182 — **The Allowlist feature is gone.** The sidebar "Allowlist" board and its `#/allowlist` route, the `crow list-allowlist` / `promote-allowlist` / `refresh-allowlist` CLI verbs, their RPC methods and the dedicated `.allowlist` dispatch lane, and the backing `AllowListService` / `AllowlistRPCSupport` / `AllowEntry` types are all removed. It existed only to view and aggregate Claude Code permission-allowlist patterns from `.claude` settings and promote a selection into the global `~/.claude/settings.json`; with most harnesses now running in auto-permission mode, nobody used it. This is a **wire-contract change**: an old CLI or web client that calls `list-allowlist` / `promote-allowlist` / `refresh-allowlist` now gets an unknown-method error. What it never touched, it still doesn't — users' actual `.claude/settings.json` permission rules are unchanged (Crow only ever read them), the **MCP tool-catalog allowlist** (`MCPToolCatalog`, the CROW-1004 security boundary) is a different concept and is untouched, and the Settings → Automation permission auto/allow **mode** toggles are a separate feature and are unaffected. The `AppState.allowListSessionID` virtual-tab UUID (`…002`) is retired.

### Fixed

- CROW-1244 — Work-session auto-complete (and `mark-in-review` / `assignedIssue` / `can_set_project_status`) now treat an `add-link --type ticket` row as the session's ticket, not only `crow set-ticket`'s `ticketURL`. A session whose GitHub issue is closed and whose PR is MERGED no longer stays Active when the ticket was attached only as a link (lived as `corveil-3296-dead-aigateway-sensor`). `add-link --type ticket` also fills `ticketURL` / `provider` / `ticketNumber` when those are unset, and hydrate backfills existing link-only sessions. Regression: `ticketURL == nil` + ticket link + MERGED PR → completion `"PR merged"`.

- CROW-1218 — A **work session is not launched without a registered worktree**. Crow auto-links PRs by matching the session's stored worktree branch; a git checkout plus managed terminal with zero Crow worktree rows left PR auto-link a no-op. `/crow-workspace` `setup.sh` now defaults `--primary` on the session's first worktree, still `die`s on `add-worktree` failure (before `new-terminal`), and refuses `launch_agent` unless `crow list-worktrees` shows a non-empty branch. The daemon marks the first `add-worktree` as primary even when the flag is omitted. `pasteDeferredLaunch` / `launchAgent` refuse a `.work` session with no registered branch (echo a warning, do not paste the agent command). Manager / review / job are unchanged. Inferring a worktree from terminal cwd is a sibling (#1219).

- CROW-1192 — The release universal build no longer goes through XCBuild. `scripts/package-release.sh` used `swift build -c release --arch arm64 --arch x86_64`, which switches SwiftPM onto Xcode's build system; under Xcode 16.4 that fails for `swift-service-lifecycle` / `swift-collections` (`supported: []` language versions + duplicate `.DependencyMetadataFileList`). Each arch is now built with the native SwiftPM build system and `lipo`d together. PR CI runs the same packaging script on `macos-15` so this class of break is caught before a tag cut.

- CROW-1157 — Claude Code's input caret no longer sits **several columns to the right of the insertion point** in a Crow terminal. xterm.js defaults to Unicode 6 cell widths, so emoji and symbols in the status line (`ctx: 0%`, mode glyphs) occupy one cell in the browser while tmux and Node's `string-width` count them as two. Claude then CUP-places the hardware cursor past the glyphs it painted; typed text still inserts at the prompt. Both live surfaces now load `@xterm/addon-unicode11` (matching the vendored 6.0.0 xterm) and set `term.unicode.activeVersion = '11'` before any PTY bytes, so those glyphs occupy the same cells the TUI laid out. `allowProposedApi` is required to switch the table; a missing `/xterm` fetch is skipped rather than taking the terminal down.

- CROW-1167 — Workspace bootstrap no longer hardcodes the ticket chip as a bare **Issue**. `setup.sh` (and the bundled template) now labels GitHub/GitLab tickets `Issue #<number>` when `--ticket-number` is numeric, matching PR chips (`PR #$N`) and orphan recovery; Jira (no standalone number) still gets `Issue`. The header chip was falling through to this stored link label whenever `ticket_badge` was missing (see #1166).

- CROW-1166 — `/crow-workspace` `setup.sh` no longer **swallows a failed `crow set-ticket`**. Ticket metadata (`ticket_url` / `ticket_number` / `ticket_title` / `provider`) is required for a ticketed workspace: `mark-in-review` and `can_set_project_status` read `session.ticketURL`, not the extra `add-link --type ticket` row that still ran after the warning. A failure now `die`s like `new-session` / `add-worktree`, keeping the RPC stderr in the setup JSON so the real error is visible. The old "may already be set" comment was wrong — `set-ticket` overwrites. Mirrored in `Resources/crow-workspace-setup.sh.template`.

- CROW-960 — Review clones no longer keep attacker-controlled harness config live for the rest of the session after the review skill's `gh pr checkout`. Crow already strips `.cursor/` / `.grok/` / `.agents/` / `.claude/settings*.json` / `.mcp.json` (and `.gemini/` / `.muse/` / `.codex/`) at launch via `prepareWorktreeForAgentLaunch`, but checkout restores those layers from the PR head into a running agent — a pre-existing window shared by every harness, only unattended from t=0 for Cursor after CROW-954. The close is **in the review skill** (and the head-advancing re-review prompt): immediately after checkout, a working-tree `rm` of the union of every harness strip surface, so one change covers Cursor, Grok, Antigravity, Claude, Muse, and Codex instead of four per-adapter patches. The files stay in the git index (do not `git rm`); inspect a stripped path that appears in the PR with `git show HEAD:<path>` or `gh pr diff`. The launch-path strip remains the next-process backstop if the agent skips the step. `scripts/check-workspace-custom-instructions.sh` pins both halves of `crow-review-pr`.

- CROW-1144 — Grok **`.work` sessions open the interactive TUI immediately** instead of running the whole ticket headlessly first. Crow launched seeded Grok work sessions as `grok --prompt-file <seed>; grok -c`, on a 2026-07-25 probe that claimed every prompt form — even a positional — forced headless mode. That is false for grok **1.0.5**: `-p`/`--prompt-file` are single-turn *headless* (a "turn" is a full agentic loop, many tool calls, then exit), and positional `[PROMPT]` is *"Initial prompt for the interactive session"*. Lived failure: the pane streamed headless narration, `grok --prompt-file` was still running 10+ minutes later, and `; grok -c` never started — Crow's terminal record was `agentLaunched`, so the bug was the command shape, not the pane. Seeded `.work` (workspace skill `launch_grok` + `GrokLauncher.launchCommand`) now uses `evalPromptLaunch` with `--` so `grok -- "<prompt>"` opens a steerable TUI from turn one. Guarded at 128 KiB (`GrokLaunchArgs.argvPromptByteLimit`, mirrored in `setup.sh`); a larger seed falls back to the headless chain rather than `ARG_MAX`. Unattended `.job`/`.review` keep `--prompt-file` then `-c` — jobs need `crow send` follow-ups and reviews need a human-gated `gh pr review`. The "one turn and exits" `GrokLaunchArgs` doc-comment is retired; [ADR 0015](docs/adr/0015-harness-capability-tiers.md) records the amendment.

- CROW-1047 — Wheel-up in a Claude Code session no longer **snaps to the start of the transcript**. Two independent paths caused the same symptom. (1) **Alt-buffer latch cap-to-0:** while an alt-buffer build is still unlatched, xterm keeps the transitional 50k; wheel-up walks that local buffer; when CROW-1023's poll finally latches and `applySurfaceScrollback` caps to 0, truncating mid-history resets `ydisp` to 0 on the oldest surviving lines — session start. The cap now **defers until `viewportY` reaches `baseY`**, then applies on the next bind. (2) **SGR burst:** line/page `deltaMode` values could yield many notches per DOM event; forwarded as repeated `\x1b[<64;1;1M` reports, Claude's mouse-tracking scroll flew to the top. `wheelNotches` now magnitude-snaps line and page mode like pixel mode (one detent in → one notch out). `hydrateWhenIdle` also skips every `agent_surface` as defense in depth beside CROW-1048's `fireScrollbackCapture` gate. Cursor/shell wheel behavior unchanged.

- CROW-1035 (reopened) — Switching back to a Claude Code session no longer parks the caret below the input box, and switching to Cursor no longer stacks footer chrome or shifts the viewport a line. #1036 moved agent tab/session switches in-place instead of the #673 full reload, but still called `term.reset()` before `select-window` and still replayed CROW-606 scrollback for inline agents. `term.reset()` clears parser state tmux re-sends only as deltas (same class of bug as CROW-1043's dead wheel), so an alt-buffer Claude's live redraw after reset misaligned the TUI; inline agents got a `capture-pane` dump racing the live attach — the same stacking #1048 fixed for mid-session hydrate. In-place switches now clear with `ESC[H ESC[2J ESC[3J]` instead of reset and send `select-window` with `replay: false`; connect/reload/heal paths are unchanged. Plain shell tabs still take the full reload.

- CROW-1048 — Cursor idle/prompt chrome (tips, model line, cwd) no longer **stacks a second copy** after ordinary tool output. #1014 restored the wheel by keeping the unified 50k on inline agents (`cursor-agent` never issues smcup); that also left Cursor on the CROW-934/CROW-1026 hydrate axis a shell uses to fill holes in a thinned live stream. Cursor's live bytes *are* the transcript, so 400ms after a tool call goes quiet the idle heal re-injected a `capture-pane` replay onto an already-painted TUI — the replay tail is the chrome, the live attach paints it again. Arrival-at-top hydrate did the same thing to history, and `switchAgentWindow` arming the CROW-1027 heal added a second replay 250ms later on every in-place switch (no PTY resize to heal). Mid-session re-capture now skips every `agent_surface`; connect/reload still restore CROW-606 history and still heal a one-screen attach. xterm stays at 50k so the wheel still works — the #1008/#1009 cap is not back. Claude's alt-buffer path is unchanged.

- CROW-1043 — A Claude Code (or Manager) session no longer goes **unscrollable after switching away and back**. #1035 made an agent tab/session switch happen in place — `term.reset()` then `select-window` on the live socket — instead of the #673 full reload, which fixed the caret jump but introduced this: `term.reset()` clears xterm's `mouseTrackingMode`, and for an **alt-buffer** agent that mode is the *only* scroll path. An alt-buffer Claude has its xterm `scrollback` capped to 0 (no local buffer, no bar); the wheel works solely because `appOwnsScroll()` is `agent_surface && mouseTracking` and the tick is forwarded to Claude as an SGR wheel button (#850). The `select-window` redraw does **not** put the mode back, because tmux emits mouse-mode changes to a client as **deltas** — switching between two agent windows that share the same mouse state (Claude → Claude, or the Manager; both `mouse_any=1 mouse_sgr=1`) repaints the frame but no mouse DECSET, since from tmux's view the client already has that mode while the client just cleared it. So `appOwnsScroll()` returned `false`, the wheel fell to a zero-length local viewport, and the surface was dead to the wheel. An explicit **Reload** recovered it because a reload is a *fresh* grouped attach where tmux re-emits the full initial state (`?1000h`/`?1006h`) — and the next in-place switch killed it again, which is the "Reload fixes it, switch away and back and it's dead" report. Reproduced live against build `2.1.233`: one round-trip switch between two Claude sessions took `mouseTrackingMode` from `any` to `none` and `appOwnsScroll()` from `true` to `false`. **The daemon now re-arms the pane's actual mouse mode on every `select-window`**: `TerminalWebSocket` reads `#{mouse_standard_flag}#{mouse_button_flag}#{mouse_any_flag}#{mouse_utf8_flag}#{mouse_sgr_flag}` and yields the matching DECSET enables (`TerminalCockpit.mouseModeReArmSequence` → `?1003h?1006h` for a Claude pane) into the same stream as the replay, so the client's mode matches the pane deterministically regardless of tmux's per-client delta. It is inert where it isn't needed — a shell (or an agent idling without tracking) reports no mouse flag, so nothing is emitted, and on a non-agent surface `swallowMouseMode` drops DECSET 1000–1016 anyway — idempotent across the connect select and the CROW-934/1027 hydrate/heal re-selects, and **visually inert** (it sets an input mode and paints nothing), so unlike a scrollback replay it can't race the live frame or blank the pane. This is deliberately **orthogonal to the CROW-606 replay**, which stays skipped for an alt-buffer pane: an alt-buffer Claude has `history_size=0`, so there is no unified scrollback to restore — the dead wheel was the mouse mode, not the replay. Inline agents and shells (`alternate_on=0`) are unaffected: their replay already runs and their wheel scrolls the local viewport regardless of mouse mode.

- CROW-1035 — Switching to a Claude Code session no longer parks the caret below the input box, and switching to Cursor no longer stacks a second copy of the footer chrome. Session/tab switch on an **agent** surface was taking the #673 full reload: a new PTY at tmux's default 24×80, a SIGWINCH to the real size, then a `capture-pane` replay injected into the same stream as the live attach redraw. On an alt-buffer pane `capture-pane` is only the current frame, so that second paint left xterm's cursor — and Claude's idea of the layout — one row below the TUI. Cursor (inline) got the same-size SIGWINCH as a reprint and a doubled last line. Agent tabs now switch in place (`clearTermBuffer` + `select-window` with `replay: false` on the live socket, no new PTY). The daemon also skips the CROW-606 replay when `#{alternate_on}=1`; the live attach already has the frame. Plain shell tabs still take the #673 reload, and the header/right-click Reload button is unchanged. *(Superseded within this unreleased block by the reopened fix above, which addresses the regression after #1043/#1046/#1048.)*

- CROW-1002 — Shift+Tab reaches the agent again, so Claude Code (and Cursor, and Codex) can cycle permission modes. CROW-980 moved the switcher off `shift+tab` by changing the **default**, and a default reaches only a config with no `switcher.binding` key at all — every install that had already written one (which is every install whose config had ever been saved, since `AppConfig` encodes the whole struct) kept the old value and went on swallowing the keystroke in every focused terminal, `captureInTerminal` being true by default. The symptom was indistinguishable from an agent bug: the chord did nothing, in every session, on a Crow that had shipped a changelog entry saying it was fixed. Three changes so it cannot recur. **The chord is reserved**: `SwitcherSettings.reservedBindings` names it, `crow ui set --switcher-binding shift+tab` is rejected with an explanation rather than stored, and a config still carrying it is migrated to the default on decode — matched case-folded and trimmed, since the setter stores the string verbatim while the client parses it case-insensitively, so `Shift+Tab ` would otherwise slip past as the same swallowed keystroke. Migration and validation deliberately share one set: a binding the decoder silently rewrites but the setter still accepts would revert on the next daemon start with nothing to explain why, which is a worse bug than the one being fixed. **The terminal keeps it regardless of configuration**: `switcherCapturesInTerminal()` subtracts the reserved chords from the `captureInTerminal` preference, so both key paths — the document-level capture listener and xterm's `attachCustomKeyEventHandler` — hand Shift+Tab to the PTY even when a hand-edited `config.json` asks for capture, and even in a browser still holding the pre-migration config it was handed before the daemon restarted. The gate is chord-scoped, not a blanket opt-out: it compares parsed chords (modifiers *and* prefix), so a bare `tab` and `esc+tab` are correctly left alone, and every non-reserved binding is still captured on the same path. **Only the binding is rewritten** — the rest of a user's switcher settings survive the migration, and any binding that isn't reserved is a real choice and is left alone. Worth knowing for anyone who liked the old chord: there is no way to get it back, by design. It buys one keystroke for a control that already has three other ways in (the sidebar, a click, the configured chord outside the terminal), and costs the agent a control with no alternative binding at all.

- CROW-969 — Gateway header values wrapped in literal quotes are rejected instead of silently breaking every Claude session. A value saved as `"Bearer sk-…"` — a shell-quoting slip, two characters longer than it should be — was stored and transmitted verbatim: `validateHeaderLine` checked only for newlines and a colon, `GatewayResolver.serializeHeaders` interpolated it as-is, and `ClaudeLaunchArgs.gatewayEnvPrefix` shell-quoted the whole value, so the stray quotes survived intact into `ANTHROPIC_CUSTOM_HEADERS`. Claude Code sent a malformed credential, the gateway rejected it, and the agent reported a bare **"API error"** naming nothing — not the gateway, not the workspace, not the header. The failure was also **invisible for as long as the role ran on Cursor**: `CrowCursor` (like `CrowOpenCode` and `CrowAntigravity`) never applies the workspace gateway at all, so a config broken months ago becomes a hard failure the moment `agentsByKind.review` is cleared to `claude-code` — which reads as an agent bug rather than a config one. The rule is now two predicates on `WorkspaceGateway` in CrowCore, shared by the CLI's `validateHeaderLine` and by `SecretRoutes.buildGateway` — the one server-side chokepoint that sees the final header map from **both** the `gateway-set` RPC and the web editor's `/config/*-gateway` POSTs, so the CLI and the browser can't drift on what they accept or what they say. A value is rejected when it is wrapped in a matching `"` or `'` (two characters minimum, so a lone quote isn't read as matching itself); a *name* is rejected for a `"` anywhere, since RFC 9110's `tchar` excludes it outright, and for a **leading** `'` only, since `'` is a legal token character. That name rule is not redundant: quoting the whole pair (`--header '"X-Api-Key: sk-…"'`) splits on the first colon into a name and value where **neither half is individually wrapped**, so nothing else catches it. Deliberately still accepted: blank values (the load-bearing "keep the stored secret" signal that lets a base URL change without restating credentials), interior quotes, JSON-ish values, and `op://` references. Three places the check deliberately does **not** live — `SecretsRPC.decodeHeaderLines`, which runs strictly upstream of `buildGateway` and would shadow its message; `WorkspaceGateway.init(from:)`, where a throwing decode makes `ConfigStore.loadConfig` return nil and the next write replaces every workspace, job and credential with defaults; and `mergingPreservedHeaders`, which substitutes *stored* values for blank ones, so checking there would make the documented base-URL-only edit fail on any pre-existing bad secret and lock the user out of fixing the very config the rule exists for. Values **already** stored this way still launch and are warned about rather than repaired: stripping would mean guessing the quotes aren't part of the secret, and a loud rejection at the gateway beats authenticating with a credential nobody verified. The warning is checked before the `op://` branch on purpose — quotes around a reference defeat `hasPrefix("op://")`, so it silently falls into the plaintext arm and is never resolved at all, which is the most confusing case of the lot.
- CROW-969 — Gateway resolution says which workspace it picked, and how. `workspaceGatewayResolved` matched a workspace two ways and then discarded it, so nothing reported where a session's gateway came from. The match is now a pure `SessionService.gatewayMatch(worktreePath:prLinks:devRoot:config:)` that both the launch path and `crow get-session` share. Every Claude launch and handoff logs one line naming the workspace, which lookup claimed it (`worktree_path` or `repo_slug`), the base URL, and the header **names** — never values; `get-session` gains `workspace_id`, `workspace_name`, `workspace_match`, `gateway_set` and `gateway_base_url`, following `WorkspaceRPC.workspaceJSON`'s redaction exactly because `get-session` is *not* in `localOnlyDenial` and a remote `/rpc` peer reads it (header names and values stay with the local-only `crow gateway get`). Manager sessions report their own gateway under `workspace_match: "manager"` rather than an empty workspace match, which would have recreated this ticket's blind spot inside the tool built to close it. This makes visible a divergence that previously had to be inferred: work and job sessions resolve **by worktree path** while review clones resolve **by repo slug** (the CROW-891 fallback, since a review clone lives under `crow-reviews/` and path math names no workspace), so a repo cloned under one workspace but claimed by another workspace's `alwaysInclude` gets a *different gateway* depending on session kind — which is why reviews could fail while work sessions on the same repo, on the same machine, kept working. Carrying the matched workspace's gateway even when it is nil also separates "no workspace claims this session" from "a workspace claims it and has no gateway": identical behaviour (`ANTHROPIC_*` unset), different fixes, previously indistinguishable. Side benefit: the two-lookup rule is now directly unit-testable — `SessionServiceGatewayResolutionTests` previously documented that it *couldn't* be, since `ConfigStore` is all-static and ADR 0012 forbids touching the live devroot pointer, so only the individual helpers were covered and their composition never was.

- CROW-956 — Settings saves and large terminal pastes no longer die on the WebSocket frame limit. `crowd` built its upgrade channel with no WebSocket configuration, leaving NIO's frame decoder at its default `maxFrameSize` of 16 KiB; anything larger was answered with close code 1009 and a dead socket. Both `/rpc` and `/terminal` already declared `inbound.messages(maxSize: 1 << 20)`, but that ceiling was unreachable from a browser: `WSCore` measures a message against `maxSize` only while appending *continuation* frames, never the first one, and a browser sends one unfragmented frame per `ws.send()`. So `set-config` — a whole-blob replace that JSON-string-escapes the entire `AppConfig`, roughly doubling it on the wire — failed for **every** field once `config.json` passed ~8 KB (a handful of scheduled jobs gets there on its own), and pasting >16 KB into the web terminal dropped the socket and lost the paste. Both were silent and misattributed: the client reconnected a second later so the UI looked healthy, and the only symptom was a save that would not stick. The ceiling is now one shared constant (`CrowDaemon.maxWebSocketFrameSize`, 1 MiB) feeding the channel *and* both handlers, matching `SocketServer.maxMessageSize` so a request's fate no longer depends on whether it arrived over the Unix socket or the WebSocket — which is why `crow workspace edit` worked while the Settings tab did not. **A message past the new ceiling still closes with 1009, and deliberately so:** no request id is ever decoded for a message that was never assembled, so the only legal JSON-RPC reply would be `id: null`, which the client correctly treats as a notification and drops — there is nothing at that layer to correlate an error to. What changes is that the close is now legible. The client reads the close code and reports `set-config was too large for the daemon, which closed the connection (1009)` instead of a bare `rpc: connection closed` (tagged `err.rpcTooLarge` for callers, with an equivalent notice on the terminal socket); every other close still reads exactly as it did, and a 1009 still reconnects, since the limit is per-message, not per-connection. Daemon-side, an abnormal read now logs `[crowd] WARNING: /rpc read ended abnormally …` where previously there was no trace at all, and the read error is propagated rather than discarded with its task — without that, a fragmented over-limit message closed as if the client had merely gone away, so the client's own 1009 signal never fired. Note the log covers the fragmented case only: a single oversized *frame* is rejected by NIO below the handler, and the client's close code is the sole signal for it. Two side effects worth knowing: `maxFrameSize` also drives *outbound* fragmentation, so `crowd` now emits frames up to 1 MiB instead of chunking at 16 KiB, and the decoder may buffer up to 1 MiB per connection while accumulating a frame, against 16 KiB before.

### Added

- CROW-1033 — **Muse Code** (Meta's `muse` CLI) is a Crow harness. New `CrowMuse` package (`MuseAgent`, `MuseLaunchArgs`, hook writer, signal source) registered in `CrowDaemon.registerAgents` as *known*, available only when `muse` resolves on PATH (or `defaults.binaries.muse`) and passes the identity probe — the token collides with the Muse Sequencer, so a foreign `muse` is greyed out in the pickers rather than launched (#879 surface-but-disable). Work / Manager launch the interactive TUI; `.job`/`.review` use `muse exec --prompt-file` then `muse resume`. Auto-permission is the bounded `--disable-approval` (sandbox stays); Crow never emits `--yolo` or `--disable-sandbox`. Review is strip-not-trust: `--trust-workspace` is withheld and `.muse/` + `.agents/` are stripped on every launch path (project memory loads even in an untrusted workspace). `--subagent-worktree-isolation` is never passed (it would create git worktrees outside the session tree). MCP is deferred (`acli` fallback). Rename is opt-out `nil` (no documented `/rename`). Wired against official docs dated 2026-08-14, not a local `muse --help` — the installer is Meta-auth-gated — so several cells are tagged needs-eval / wire-worthy in the [capability matrix](docs/agent-harness-matrix.md). Self-host is a **permanent** Tier-2 gap (closed-source, Meta-auth-locked), same class as Antigravity. [ADR 0015](docs/adr/0015-harness-capability-tiers.md) records the amendment.

- CROW-1030 — The build SHA in **Settings → About** is a link to that commit on `corveil/crow`, and the desktop app grows a **Crow → Build on GitHub** menu item pointing at the same commit. The SHA was already stamped and displayed on both surfaces; it just wasn't reachable, so answering "what is actually running here?" meant copying seven characters into a URL by hand. The decision of *whether* to link lives in one predicate — `crowCommitURL` in `app.js`, mirrored as `crow_commit_url` in the Tauri shell — which returns nothing for any stamp that would land on a 404: `dev` (built outside a git checkout), an absent or blank stamp, and anything non-hex or outside git's 7–40 character range. Its bounds match `VersionUpdateClient.githubCompareURL`, which already guards the same stamp Swift-side for the compare URL, so all three agree on what counts as a commit. An unlinkable stamp renders as **plain text on the web and a disabled menu item on the desktop** rather than a link that 404s, which is the acceptance criterion and also the reason the check is a predicate returning `nil` rather than a boolean beside a separately-built string. The web link **displays the short SHA but hrefs the full one** (`gitShaFull`, stamped into `version.json` since CROW-938), so the visible line is unchanged while the URL is unambiguous; the desktop gained a matching `CROW_GIT_SHA_FULL` from `build.rs` for the same reason. **The desktop's control sits beside About rather than inside it**, and that is a platform limit, not a preference: macOS draws the About panel itself (`NSAboutPanel`) and muda hands it plain strings — `credits` reaches it as an `NSAttributedString` built by `from_nsstring`, with no link attribute — so no part of that panel's text can be made clickable from Tauri. A menu item one row down is the closest reachable control, and it opens through `tauri_plugin_opener`'s Rust-side `open_url`, which is not ACL-gated the way the frontend IPC command is. One case this deliberately does **not** catch: a SHA that is well-formed but exists only locally (an unpushed commit, or a build from a fork) still links, and GitHub will 404 it — detecting that needs a network round-trip, which is what the version-update check already does on its own schedule and is not worth blocking a menu item on.
- CROW-1011 — Settings → Corveil CLI has **Verify** and **Reinstall skill** again. Both shipped in the macOS app (CROW-482/490/491) and were lost when it was retired ([ADR 0010](docs/adr/0010-retire-the-macos-app.md)); the web tab replaced them with a line reading *"Verify/Reinstall are available in the desktop app"*, which has pointed at nothing that exists since. **Verify** runs `<path> --version` and reports the first line back; **Reinstall skill** re-runs the same `corveil skill install` the daemon runs at launch, writing `{devRoot}/.claude/commands/query-corveil.md` — the "I just rebuilt corveil locally, pick up its new embedded skill" loop, without restarting `crowd`. Both act on the value **currently in the field, saved or not**, which is the point of a Verify button: you check a path before committing it. The behaviour lives in `CorveilCLI` (CrowEngine) rather than in the UI, because there are two doors to it — `POST /config/corveil` for the browser and the `corveil-verify` / `corveil-reinstall-skill` RPCs behind the new **`crow corveil verify`** / **`crow corveil reinstall-skill`** verbs — and a decision made in one door is a decision the other drifts from; same shape as `MCPTokenRPC`. Reinstall is thin over `Scaffolder.installCorveilSkill`, the launch path itself, so "reinstall and see if that fixes it" stays a real diagnosis rather than testing a second implementation, and both surfaces update `corveilSkillInstallWarning` — succeeding clears the startup warning, failing replaces it — so "is corveil broken?" has one answer instead of a launch answer and a button answer. Both RPCs are **local-only** on `/rpc` and the buttons render only for a local browser: they execute an absolute path on the daemon host, which is the arbitrary-execution half of the capability that already makes `defaults.binaries` local-only to *write*, reachable from an ungated read of the same config — the argument that gates `open-in-vscode` / `open-terminal`. Remote peers keep today's read-only path field. A broken binary answers **200 with `ok: false`**, not an HTTP error, and the CLI exits zero: missing, non-executable, non-zero exit, or hung past the 5s ceiling are all successful *reports* of a broken binary, so callers branch on `ok` and a non-zero `crow` exit keeps meaning the request never ran. The 5s ceiling and its watchdog are `Scaffolder`'s, not a copy — a corveil that hangs on `--version` is bounded exactly like one that hangs on `skill install` — and both actions run on a detached task so that wait cannot hold a cooperative thread the daemon serves other sessions from.
- CROW-1004 — `crowd` serves a **read-only MCP surface**, so clients that speak the Model Context Protocol (Cowork locally, a Grok bot off-box) can read the board without a Crow-launched agent session. Six tools — `get_board_summary`, `list_sessions`, `get_session`, `list_stuck_sessions`, `list_tickets`, `list_reviews` — over five read RPCs. There is **no prompt-send, no session creation, and no config access**, and that is structural rather than a policy: the tool catalog is a closed allowlist with no passthrough, so an RPC method cannot be named from the wire and the local-only surfaces (`gateway-*`, `web-password-*`, `mcp-token-*`, `run-setup`, `hook-event`, host-app launches) are unreachable regardless of transport or scope. **The surface is generated from an allowlist the `ParityLedger` gates**: `RPCEntry` gained `mcp: MCPExposure`, defaulting to *not exported*, so a newly registered RPC cannot silently become a Grok-reachable tool — it stays invisible until someone types `mcp: .read(scope:)` on its ledger row, which is the diff a reviewer reads. `MCPLedgerExportTests` fails the build when a tool reads an unflagged method, when a flagged method has no tool, when an exported method is a write, or when an exported method is local-only; that last one is the acceptance criterion "local-only RPCs remain unreachable over the remote MCP path", asserted rather than inspected. **Two transports, two trust models.** `crow mcp serve` bridges stdio to the 0600 Unix socket with no token — a caller who can run it can already run every other `crow` verb, so a token would gate nothing — while off-box clients POST to `/mcp` with a scoped bearer token from `crow mcp token mint` (also in Settings → Web access, local browser only). `/mcp` is exempt from `WebAuthMiddleware` and authenticates itself, which is **stricter**, not looser: that middleware gates on the web-session cookie and is opt-in, so on a daemon with no web password it would wave anyone through, whereas `/mcp` has no tokenless path and no loopback bypass. Scopes are exactly `sessions:read` and `board:read` — no `sessions:write`, no `prompt:send`, no `admin`, not deferred but *undefined*, so a token cannot name a capability the server might later grow into — and `tools/list` is filtered **by the presented token**, since a deny-after-call still teaches the model that hidden tools exist. Tokens expire in 90 days by default (`--no-expiry` must be typed; `--expires-in 90` is rejected as ambiguous between seconds and days) and are stored as a SHA-256 hash with a displayable prefix, so there is no `--reveal` and a lost token is replaced rather than recovered. Deliberately **not** PBKDF2 like the web password: that protects a low-entropy human secret and is verified once per login, whereas a token is 32 CSPRNG bytes verified on every request, where 210,000 iterations would be a self-inflicted denial of service — same reasoning as a GitHub PAT, and constant-time compared either way. `SettingsSecrets` strips token hashes on the way to a browser and **restores them from disk on the way back**, so a `set-config` round-trip can neither mint a token nor revoke one. Two tools earn their shape rather than proxying a handler: `list_stuck_sessions` joins `list-sessions` and `list-sessions-live` — disjoint payloads, so neither answers "what needs a human" alone — and reports `waiting_on_input` / `idle_too_long` / `checks_failing` / `auto_merge_stuck` / `auto_rebase_stuck` with each verdict's own message; `list_tickets` drops the issue `body`, the one unbounded field in that payload, which would otherwise let a single long issue crowd the board out of the model's context. The server is **dual-era**: revision `2026-07-28` replaced the `initialize` handshake with per-request metadata and a mandatory `server/discover`, but it is weeks old and shipping clients still open with the handshake, so both are answered on one endpoint and the era is decided per request. See [`docs/mcp.md`](docs/mcp.md) for client setup and [ADR 0019](docs/adr/0019-read-only-mcp-server.md) for why the surface is task-shaped rather than one tool per RPC — including why this does not reverse CROW-528, which is about Crow *provisioning* MCP into harnesses, the opposite direction on the same wire.
- CROW-974 — Pre-submit guardrails in `crow-review-pr` block mis-targeted and verdict-inconsistent reviews before `gh pr review`. A review body that cites file paths with zero overlap against the target PR's changed-files list must not be posted (catches reviews drafted from carried-over context about a different PR). The `**Recommendation:**` line must agree with the submit flag (`Approve` ↔ `--approve`, `Request Changes` ↔ `--request-changes`). Both checks are local to the skill — advisory, like the CROW-963 verdict policy — and reach every harness through the shared `skills/crow-review-pr/SKILL.md` / `Resources/crow-review-pr-SKILL.md.template` pair (Claude reads the copied skill; Cursor/Codex/OpenCode/Grok/Antigravity read the inlined body). `scripts/check-workspace-custom-instructions.sh` now requires the guardrail section in both halves.
- CROW-963 — Per-workspace review verdict severity policy. Which `crow-review-pr` finding severities force `gh pr review --request-changes` is now a workspace setting (`reviewBlockingSeverities`) instead of prose hardcoded in the bundled skill, so a repo where Yellow means "nitpick" can request changes on Red only while a repo where Yellow means "should fix" keeps blocking on both. Settable with `crow workspace add|edit --review-blocking-severity red|yellow|green` (repeatable), readable via `workspace get`/`list`, and editable in Settings → Workspaces → Review verdict. **Unset means today's behaviour** (`red` + `yellow`), not "nothing blocks", and `--clear-review-blocking-severities` restores it by *removing* the key rather than writing a null or an empty list — the three states round-trip differently, and a null would make the whole `config.json` undecodable. An explicitly empty set is rejected on every write path: a workspace where nothing gates the verdict approves every review, and with the auto-merge watcher on merges it too. Decoding stays deliberately lenient in the other direction — a hand-edited config with an unknown severity drops it and falls back to the default rather than throwing, because a throwing `WorkspaceInfo` decode makes `ConfigStore.loadConfig` return nil and every writer's `?? AppConfig()` fallback then overwrites `config.json` with defaults. The policy is rendered into the skill body **once, before the review launch paths diverge**, so it reaches Claude (which reads the copied `.claude/skills/crow-review-pr/SKILL.md`) and Cursor/Codex/OpenCode/Grok/Antigravity (which read the whole body inlined into `.crow-review-prompt.md`) alike — hooking only the file copy would have left the inlining agents on the default rule, which is the common case whenever `agentsByKind.review` is Cursor. Both the Step 5 verdict rule and the summary table's "Verdict effect" column are generated from the same policy so the brief can't contradict itself, and a severity that stops gating still reads `Approve allowed` and is still reported in the review body — only the verdict changes. Two things this deliberately is **not**: it does not touch the Red/Yellow/Green taxonomy, and it is **advisory rather than enforcement** — the agent invokes `gh pr review` itself, so Crow renders the policy into the prompt but never sees the call and cannot validate the verdict actually posted. Worth knowing before relaxing a workspace: more PRs reaching an approved state feeds `autoMergeWatcherEnabled` and the `crow:merge` gate, so a workspace set to `["red"]` will auto-merge things it previously would not have. A regression test pins the default rendering byte-for-byte against the pre-change skill text, and `scripts/check-workspace-custom-instructions.sh` now diffs `skills/crow-review-pr/SKILL.md` against `Resources/crow-review-pr-SKILL.md.template` so the two halves (dev builds read the first, release builds the second) can no longer drift apart silently.
- CROW-938 — Detect when a newer Crow build is available by comparing the stamped `gitSha` against `corveil/crow` `main` (no release tags required). `crowd` owns the GitHub compare call, caches the result, and checks at most once per daemon start plus every ≥1 hour (configurable, opt-out via `versionUpdate.enabled`). Surfaces: `crow version --check` / `crow version check` (human summary with exit codes), `crow version get|set`, Settings → About (status + Check now), and a dismissible web banner when behind. Offline, rate-limited, `dev`, or unknown SHAs report `unknown` — never a false up-to-date.

## [0.2.0] - 2026-08-05

Backfill of merged PRs since the 0.1.0 release, grouped by theme.

### Web UI

- CROW-936 — The web UI has real URLs. Selecting a session, switching terminal tabs, opening the Ticket/Reviews/Allowlist/Scorecard boards, and each Settings tab are all addressable, so views can be linked, bookmarked, and survive a reload, and Back/Forward move between them instead of leaving the app. Routing is **hash-based** ([ADR 0018](docs/adr/0018-web-client-hash-routing.md)) — `crowd` serves the client from a table of exact literal paths with no catch-all, so under History-API routing an *authenticated* cold load of `/sessions/<id>` would 404 in the router, while an unauthenticated one renders the login page (the auth middleware answers any navigational GET with a 200), and the Tauri shell's `location.reload()` would hit the same 404. A fragment never reaches the server, so no Swift file changes. Routed navigation goes *through* the existing `selectSession` / `selectBoard` / `switchTerminal` / `openSettings` entry points rather than beside them, and `navigate()` no-ops on an unchanged hash so applying a route can't push a second history entry. Two shapes the ticket's sketch got wrong are corrected: there are four boards, not two, and Jobs is a Settings tab (`#/settings/jobs`), not a screen. A URL naming a session that no longer exists now lands on an explicit not-found card with a way back — Crow's retention reaper deletes completed sessions, so a dead link is the normal fate of a shared URL, and previously it left a blank header, an orphan "+" tab, and a frozen terminal; the same guard now also catches a session reaped *while you're viewing it*, which nothing checked before. Deep links survive the login round-trip: the auth middleware serves the login page at the requested URL, so the fragment is still in the address bar and is handed back once the password is accepted. Only the addressable view is encoded — scroll position, open menus, selection mode, and board filters stay out of the URL.

- #879 — Agent pickers now show **all** known coding agents, greying out (and disabling) the ones whose binary isn't on the daemon's PATH instead of hiding them — so a shipped-but-uninstalled agent (e.g. Antigravity) is discoverable, with a "not found on PATH — install it and restart Crow to enable" tooltip explaining why it's disabled. Covers the new-manager launch menu, the agent-handoff menu, and every Settings selector (Default agent + the four per-action overrides). `list-agents` gained `available` + `binary` fields; the launch gate is unchanged, so an off-PATH agent still can't be launched or handed off to.
- #752 — Multi-select "Start Working (N)" on the tickets board now fires a single `batch-work-on-issues` RPC, so the Manager runs one `/crow-batch-workspace url1 url2 …` (parallel setup) instead of N separate `/crow-workspace` submissions.
- #613 — Left pane paints chrome + skeleton (or cached last-known) session rows on first load before `list-sessions` returns, then swaps in real rows without a blank flash.
- #768 — Notifications for automation events are back after the native→web move: auto-workspace created, auto-merge enabled, auto-rebase pushed, auto-rebase conflicts, and config reloaded. The daemon pushes each over the existing `/rpc` socket at the moment the watcher acts; the web UI chimes and posts a browser notification, with per-event Enabled / sound / system toggles under Settings → Notifications. Conflict events use a distinct, harsher tone and are never suppressed by the "you're looking at it" focus rule.

### Automation

- CROW-876 — `mark-in-review` now moves the provider's board, not just Crow's session status. `IssueTracker.markInReview` did the real work but had no callers: its only entry point was `AppState.onMarkInReview`, which nothing assigned once the macOS app was retired (ADR 0010), so the verb silently degraded to `set-status inReview` while the web menu still gated it on `can_set_project_status` — promising a board move that never happened. That left the board pipeline with two of three legs wired (session start → In Progress via `setup.sh`, issue done → Done via `closeTask`), and `crow resync-jira` as the only route to In Review. The handler now calls the tracker like `mark-issue-done` does: board first, so a failed transition exits non-zero instead of leaving the session marked, then the session write. Preconditions and provider failures come back as typed `SessionActionError`s rather than a silent early return, which also fixed a latent defect the method carried unnoticed while dead — it resolved its backend from the bare provider enum, dropping the host for self-hosted GitLab/Corveil. A provider with no In Review status to move *to* is deliberately **not** an error: nothing was ever going to move, so the session still transitions and the response carries an additive `warning`. That covers GitLab and, more importantly, any GitHub board whose column isn't named "In Review"/"Review" or whose Status field is unset — throwing there would have turned a fix into a breakage for a large slice of real boards. **New side effect:** on a GitHub repo with no project board at all, `mark-in-review` now applies the `crow:in-review` fallback label (#706, #790) — a repo write this verb never made before. `docs/automation.md` also claimed a "PR opened → In Review" transition that no code performed; corrected rather than implemented.
- CROW-921 — A changes-requested PR whose findings were actually fixed no longer parks in `changesRequested` forever. The loop had two states and needed three: the only thing that re-requested review was a sentence inside the changes-requested prompt, and that prompt is reachable only while the agent still *owes* a fix — so it could never fire once the fix landed, and with the host's review request already consumed the PR was invisible to the reviewer's queue and to `review-requested:@me` alike. `PRStatus.changesRequestedState` now names all three (`needsRefine` / `awaitingReRequest` / `awaitingReviewer`) and both `needsRefine` and the new `applyAutoReRequestReview` watcher derive from it, so they can't drift apart. The third state is scoped to *the reviewers who requested changes*, not to "does the PR have any pending request" — requests are per-reviewer and the host clears only the submitting one, so a PR routinely carries A's blocking verdict alongside B's still-pending original request, and a PR-wide reading would silence both halves of the loop for exactly the multi-reviewer PRs this is meant to rescue. The watcher calls `gh pr edit --add-reviewer` from the daemon rather than asking the agent to — a one-line API call routed through an agent turn costs a turn's tokens, can't be verified, and would re-inherit the gate that caused the dead-end; the prompt hint stays as belt-and-braces, since re-requesting an already-requested reviewer is a no-op. Gated by `autoRespond.autoReRequestReview` (**on** by default, alongside `respondToChangesRequested`, because the prompt has always instructed the agent to do this), readable and writable via `crow automation get|set --auto-re-request-review`. Two supporting fixes: `lastSubstantiveCommitAt` now anchors on the commit **author** date rather than the committer date, closing the documented CROW-508 rebase gap in both directions — a rebase restamp no longer suppresses a genuine needs-refine, nor fires a no-op review round at a reviewer; and every *suppressed* needs-refine evaluation is now logged with the gate that held it (`needs-refine: #N gated (reason=…, state=…, lastCR=…, lastCommit=…, reviewerReRequested=…, idle=…, cooldown=…)`), rate-limited to changes plus an hourly heartbeat, where previously only firing was logged and a stuck PR left no trace at all.
- #888 — The `crow:merge` label no longer lands in silence. When Crow's auto-merge watcher can't act on a labeled PR, the reason now reaches the UI instead of only `crowd-automation.log`: `list-sessions-live` carries an additive `auto_merge_state` (`{phase, reason, message, permanent}`), and the sidebar PR pill and detail header render it as a tinted ⛙ chip in the shared PR glyph vocabulary — green enabled, purple merged by Crow, orange retrying, red permanently blocked, grey watcher-off — with the full sentence in the tooltip and `aria-label`. The twelve skip reasons (previously six enum cases plus six ad-hoc string literals) are one enum carrying the same grep-stable log strings plus a human sentence and a permanence flag; reasons the pill already renders (conflicts, changes-requested, no label) are deliberately not republished. Repo policy is now read proactively from GraphQL `Repository.autoMergeAllowed` on the fetch Crow already makes, rather than discovered by failing `gh pr merge --auto` and string-matching the error, and a repo with GitHub's "Allow auto-merge" disabled gets a **direct squash-merge fallback** for PRs that are OPEN, non-draft, `CLEAN`, `MERGEABLE`, checks-green, approved, and Crow-authored. A permanent block fires a new **Auto-Merge Blocked** notification, once per PR rather than once per 60s poll. `crow add-merge-label` still applies the label and still returns `ok: true`, but now includes a `warning` when the label can't lead to a merge. The bare untinted ⛙ in the session row's trailing cluster is folded into the PR pill, so "Crow armed this" and "Crow gave up on this" no longer render identically.
- #137 — Session analytics emitted via Claude Code's OpenTelemetry exporter (configure via `CLAUDE_CODE_ENABLE_TELEMETRY` and `OTEL_EXPORTER_OTLP_ENDPOINT`).
- #163 / #165 — Add `remoteControlEnabled` setting; new sessions launch with `--rc` so they can be driven from claude.ai or the mobile app. The `/crow-workspace` skill honors the setting.
- #182 — Auto-complete now requires positive evidence that the session was worked before flipping to Completed; prevents idle sessions being marked done by an unrelated PR merge.
- #189 — Manager terminal launches in `--permission-mode auto` by default so orchestration commands (`crow`, `gh`, `git`) skip per-call approval. Toggle at Settings → Automation → Manager Terminal.
- #209 — Per-workspace opt-in to auto-start review sessions when a PR becomes reviewable.
- #211 — Auto-create a workspace when an issue assigned to you carries the `crow:auto` label.
- #213 — Auto-suggest opening a PR when a session completes its work but no PR is linked yet.
- #214 — Optional auto-respond toggles: when enabled, Crow types an instruction into the session's Claude Code terminal in response to changes-requested reviews and failed CI checks. Off by default.
- #222 — PRs opened from a Crow session are auto-labeled `crow:auto`.
- #228 — Settings split into discrete tabs; every automation toggle lives under Settings → Automation. New `docs/automation.md` covers the full lifecycle.
- #327 — Scheduled Jobs scope to a workspace + repo, where the repo picker is populated from the workspace's provider by expanding its "Always Include Repos" specs (`owner/*` globs or `owner/repo` slugs). Repos not yet checked out are cloned on demand when the job fires.
- #461 — Remove the PR auto-label workflow (reverts #222); `crow:auto` on a PR was a no-op since only assigned issues consume the label. The label itself stays for issue auto-launch.
- #814 — General-tab settings are drivable from the CLI: `crow telemetry get/set`, `crow cleanup get/set`, `crow ui get/set`. Each `set` is a patch that echoes the resulting subtree plus `restart_required` — telemetry's enabled/port need a `crowd` restart, cleanup applies within a board poll, and the sidebar preference repaints connected browsers within ~2s. Riding along: the three settings structs now decode per-key with defaults, and config writers refuse to overwrite a `config.json` that exists but won't decode, closing a path where one malformed block could silently reset every workspace, job, and credential to defaults.
- #813 — Notification settings from the CLI: `crow notifications get|set` reads and writes `AppConfig.notifications` — the global mute / sound / system-notification toggles and each of the ten events' `enabled`, sound, system-notification, and sound-name settings (validated against the 14 built-in sounds). Writes go through the shared config lock, and the `configReloaded` refresh above is awaited before the event chimes, so a mute applied from the CLI also silences the notification announcing it. The `notifications` block now decodes per-key with defaults too, so a partial hand edit no longer fails the whole config.
- #604 — Full job management from the CLI: `crow job list/get/add/edit/enable/disable/run/delete/duplicate`. Mutations route through the running app's live config (picked up by the scheduler and Settings UI without a restart), and `crow job run --id` fires a job immediately regardless of its schedule or enabled flag, returning the launched session/terminal ids.
- #810 — Workspace and automation defaults from the CLI: `crow defaults get|set` reads and writes `AppConfig.defaults` — provider, forge CLI, branch prefix, the review/ticket repo exclusions and ignored review labels, and the binary path overrides. The three lists are edited incrementally (`--add-…` / `--remove-…` / `--clear-…`, matched case-insensitively like the board filters themselves) instead of by replacement, so excluding one more repo doesn't mean restating the list. `--binary` merges into the map rather than replacing it, and is local-only — binary overrides are absolute paths that execute at the next agent launch — and returns `restart_required`, including when removing one, since nothing re-scaffolds `.claude/bin` until the next launch. `--cli` is exposed alongside `--provider` because the two are read as an independent pair and the web UI has no `cli` field at all, so a provider flip previously left a broken pair with no way to fix it short of hand-editing `config.json`.
- #812 — Settings → Automation toggles from the CLI: `crow automation get|set` reads and writes the eleven booleans behind the tab — the five auto-permission modes (including the Jobs one, so they read as a group), remote control, commit attribution, the `crow:auto` / `crow:merge` watchers, and the three `autoRespond` toggles. Booleans take an explicit `true`/`false` because six of the eleven default to on, so a bare flag could never turn one off. Nothing needs a `crowd` restart — the daemon re-reads config lazily rather than holding a snapshot — except `managerAutoPermissionMode`, which is baked into the Manager terminal's stored command and so reports `manager_restart_required` and points at `crow restart-manager`. The tab's three board-filter lists are `AppConfig.defaults` fields and belong to `crow defaults` (#810); `get` surfaces the derived `effective_exclude_review_repos` (the global list unioned with every workspace's own) so the filter that actually applies is visible from either verb.
- #817 — Board and workflow actions are no longer web-only: `crow list-tickets`, `list-reviews`, `refresh-tickets`, `work-on-issue`, `batch-work-on-issues`, `start-review`, `create-manager`, and `quick-action` drive the same RPCs as the board buttons. The read verbs print the board payload verbatim for `jq`; `batch-work-on-issues` takes repeatable `--url` plus `--urls-file -`, so a ticket list pipes straight out of `gh issue list`.

### Internal

- CROW-876 — Deleted thirteen `EngineRouter` routes that returned an unconditional `{"ok": true}` for nothing: the five session-lifecycle verbs plus `quick-action`, `work-on-issue`, `start-review`, `promote-allowlist`, `refresh-tickets`, `refresh-allowlist`, `create-manager`, and `run-job`. Every one was shadowed by a daemon handler of the same name (`CommandRouter` reaches a fallback only for an *un*registered method), and twelve of their thirteen `AppState` callbacks were never assigned anywhere — so even if routing changed, they would have reported success for a no-op. This is the same false-success shape #868 removed from the daemon path. The twelve orphaned callbacks went with them; `onCompleteSession` stays, since `IssueTracker` still uses it for auto-complete-on-merge. `SessionLifecycleRPC.requireTicketURL` was also retired — `mark-in-review` was its last caller, and that check now lives in `IssueTracker` alongside the provider, Manager, and session-existence guards, restoring the "one source of truth, not two" invariant the type documents.

### Review Board & Sessions

- #153 — Fix PR review status not reflecting the actual review state.
- #174 — Ticket card issue and PR chips are now clickable.
- #188 — "Move to Active" button on completed sessions returns them to active without deletion.
- #205 — PR link reconciliation for sessions that missed reactive detection — `gh pr list` is consulted on the next polling cycle.
- #206 — Rename terminals via the UI and via `crow rename-terminal`.
- #207 — `defaults.excludeReviewRepos` filters repos from the review board, badge counts, and notifications. Wildcards supported.
- #210 — Bulk delete for sessions in the sidebar.
- #212 — Multi-select + batch Start Review on the review board.
- #220 — Filtering for the tickets list.
- #226 — Per-section select all and icon-only cancel button in selection mode.
- #231 — Quick action buttons on the session detail header.
- #520 — PR-link reconcile no longer attaches the wrong PR when the worktree branch carries a repo-name prefix the PR head drops (`feature/max-monorepo-maxx-7035-…` vs `feature/maxx-7035-…`). The ticket key is derived from the worktree branch, key matching ignores body-only mentions, and a PR can attach to at most one session — a session whose ticket has no PR now gets none.
- #816 — The web session-menu actions are now CLI verbs: `crow mark-in-review`, `complete-session`, `set-session-active`, `mark-issue-done`, `add-merge-label`. `IssueTracker.markIssueDone` / `addMergeLabel` now throw `SessionActionError` instead of silently returning, so `mark-issue-done` / `add-merge-label` report the real reason (no ticket, no PR, no provider, missing capability, unparseable repo, or a failed provider call) rather than `{"ok":true}` for a call that did nothing. Preconditions the browser enforced by hiding menu items — including Manager rejection — moved server-side. The three status transitions now route through `SessionService`, restoring the analytics snapshot a completed session is meant to record.
- #894 — CI status (failing glyphs, **Fix Checks**, auto-respond-to-failed-checks) no longer disappears for *every* PR when the account has one open PR in a SAML-restricted org. GitHub nullifies the individual `nodes` entries it won't resolve and returns the rest, but Swift's `as? [[String: Any]]` cast is element-wise and all-or-nothing — one `null` made the whole array cast fail, so the parser dropped 8 perfectly good PRs to salvage nothing. The same trap sat on all 16 GraphQL connection walks in the two GitHub backends, including the assigned-issues board, which emptied the same way. All of them now go through a `LenientJSON` helper that filters unresolvable elements instead of failing the array. Riding along: `prStates` (the stale/session-linked PR backfill) now requests `statusCheckRollup`, so a PR past `viewer.pullRequests(first: 50)` — or one in a restricted org, which is a *permanent* hole in that connection — can show CI state at all; and it recovers its accessible aliases under SAML instead of letting one blocked repo throw away every other PR's state in the batch. One behavior change to note: an issue whose project board has a SAML-nulled item is now correctly seen as being on a board, so a missing Status option raises `unimplemented` rather than quietly writing a `crow:in-review` label.

### Terminal Runtime

- #159 — Fix diagonal window resize and content-driven window growth.
- #161 — Fix batch "Work on" sending a malformed `/crow-batch-workspace` line.
- #218 — Recover from failed Ghostty surface creation by retrying.
- #229 — New tmux backend behind the `CROW_TMUX_BACKEND` feature flag (or Settings → Experimental). Off by default; opt in for a headless-PTY runtime that decouples terminal lifecycle from view rendering.
- #301 — The tmux backend is now the default for managed terminals. The Settings → Experimental tab and `AppConfig.experimentalTmuxBackend` key are gone; existing configs containing the key still load (the key is silently dropped on next save). Escape hatch: set `CROW_TMUX_BACKEND=0` (also `false`/`no`/`off`) to fall back to the legacy per-terminal Ghostty backend for a launch — the legacy path will be removed in a follow-up release.
- #303 — Removed the legacy per-terminal Ghostty backend and the `CROW_TMUX_BACKEND` escape hatch. tmux ≥ 3.3 is now required for managed terminals; without it, Crow shows the install alert and managed terminals don't render. `TerminalManager` is gone and `TerminalRouter` no longer branches on backend. Persisted terminals tagged `"backend":"ghostty"` migrate forward to tmux on load.

### GitLab

- #215 — Fix GitLab fetch failing when `GITLAB_HOST` did not match the workspace host; reconcile now silently skips GitLab candidates whose host can't be determined.
- #233 — Fix `glab` fetch failures from a non-repo cwd and slug truncation on nested groups (`big-bang/product/packages/elasticsearch-kibana` is no longer truncated to `big-bang/product`).

### Tooling & Misc

- #939 — Fix `release.yml` to ship universal `crow` and `crowd` binaries in a tarball; remove the deleted `.app` bundle/signing steps and publish unsigned CLI artifacts (see [Unsigned Builds](docs/troubleshooting.md#unsigned-builds)).
- #809 — CLI parity for Settings → Workspaces: `crow workspace list | get | add | edit | remove` covers every `WorkspaceInfo` field — provider, GitLab host, task provider, the Jira site/key/JQL/status-map, Corveil host, custom instructions, the three repo lists, and `sessionEnv`. Five granular RPCs rather than whole-blob `set-config`, so changing one field no longer puts every workspace, job and credential shell on the wire. `--workspace` resolves by name (case-insensitive) or UUID. Three latent defects went with it: `WorkspaceInfo.validateName` had **zero** production callers — the Settings form checks only "non-blank", so duplicate and path-unsafe names were persistable — and these verbs are its first enforcement; `sessionEnv` was read by `skills/crow-workspace/setup.sh` and documented in the skill but missing from `WorkspaceInfo`, and since `encode(to:)` is synthesized from `CodingKeys`, every config save silently deleted a hand-authored block; and renaming a workspace silently orphaned its sessions (worktrees stay at `{devRoot}/{oldName}/`, so gateway resolution and code-provider detection break) and its jobs, which now refuses unless `--force` is passed and reports the counts either way. `remove` is guarded the same way, leaves the directory on disk, and says when it discarded a stored AI gateway. An edit whose values already hold reports `"saved": false` and skips the write rather than churning `config.json`. The per-workspace gateway stays with `crow gateway` (local-only) and never appears in a `workspace-*` payload.
- #819 — Allowlist and analytics are scriptable from the CLI: `crow list-allowlist`, `crow promote-allowlist --pattern …`, `crow refresh-allowlist`, `crow get-scorecard`, `crow rebuild-scorecard`, `crow get-state`, and `crow list-artifacts --session`. `promote-allowlist` now reports honestly — a failed write to `~/.claude/settings.json` returns an RPC error instead of `{"ok":true}`, and the response names which patterns were `added` vs. `already_global`. Two latent write-path bugs went with it: a global settings file that failed to parse was previously treated as empty and written back containing only `permissions.allow` (silently destroying `hooks`, `env`, `model`, `mcpServers`, and every other key) — it is now refused untouched, and the write is atomic; and a `patterns` payload holding a non-string element was filtered down to the valid subset and reported as a full success, where it is now rejected outright. `list-artifacts` additionally returns each image's absolute on-disk `path` plus the session's `dir`, so an agent that wrote via `$CROW_ARTIFACTS_DIR` can verify what landed.
- #818 — CLI parity for the tmux/daemon-maintenance actions that were web-only: `crow restart-manager`, `crow restart-tmux-server`, `crow reload-tmux-config`, `crow launch-agent --terminal`, `crow retry-readiness --terminal`, `crow open-in-vscode --session`, and `crow open-terminal --session`. Each maps 1:1 to the RPC method of the same name. The destructive restarts are unprompted from the CLI, matching `recreate-terminal`; `restart-tmux-server` returns before its background rebuild finishes.
- #805 — `crow edit-link` updates a session link's label, URL, or type in place (`--id`/`--url` selects, `--new-url` sets the new URL, only provided fields change) so a mislabeled link can be corrected without a remove-then-add round trip. Also documents the pre-existing `crow remove-link`, which detaches a link by `--id` or `--url` — it was implemented but missing from the CLI reference, skill, and CLAUDE.md.
- #769 — `crowd` can start at login again (the gap ADR-0010 left when the macOS app was retired). `crow autostart install | uninstall | status` registers a launchd LaunchAgent — idempotent, re-points itself after an upgrade, and never bootstraps a duplicate over a running daemon. Same toggle at Settings → General → Autostart from a local browser. macOS only for now; Linux waits on #645.
- #152 — Replace dock icon with the Corveil Brandmark.
- #155 — Docs refresh: README, `make build` promotion, GitHub project scope wording.
- #162 — Silence noisy console logs from Ghostty and IssueTracker.
- #172 — Log `gh` stderr on IssueTracker shell failures.
- #175 — Consolidate IssueTracker `gh` calls into a single GraphQL query.
- #176 — Open-source readiness: license, code of conduct, CI, doc cleanup.
- #178 — CI warms the Ghostty cache on `main` so PRs share it.
- #180 — Fix IssueTracker duplicate-key crash on PR status refresh.
- #185 — Replace the Corveil Brandmark PNG with an SVG for crisper rendering.
- #208 — Ignore subagent hook events fired after a turn's `Stop` so the sidebar dot doesn't get stuck "working".
- #234 — `crow hook-event` is a silent no-op when the Crow app is not running, so non-Crow `claude` sessions don't log noise.

## [0.1.0] - 2025-04-05

Initial open-source release of Crow.

### Added

- Native macOS application with ticket board, terminal management, and GitHub integration
- Embedded Ghostty terminal with multi-tab support per session
- Session-based workflow management (create, pause, resume, archive)
- Git worktree management with orphan worktree recovery
- GitHub integration with PR status tracking and project board sync
- "In Review" button to update GitHub Project status from the app
- Claude Code hook event system for automatic session activity tracking
- Notification system with configurable sounds and macOS notifications
- "Open in VS Code" and "Open Terminal" buttons for session worktrees
- CLI tool (`crow`) for session, terminal, and metadata management via Unix socket RPC
- `crow setup` command for first-time configuration
- Makefile for build automation (`make build`, `make release`)
- Corveil branding with styled About page showing git commit SHA
- Configurable sidebar with option to hide subtitle lines
- Ticket page redesign with search, sort, and done state filtering
- Claude Code allow list aggregation and promotion across worktrees
- Comprehensive README with setup guide, architecture docs, and CLI reference
- Contributing guide, issue templates, and PR template
- Security audit and open-source readiness documentation

### Fixed

- Ghostty terminal mouse position offset
- UI blocking during GitHub polling and terminal resize on display change
- Merged PR status not detected on app restart
- Crash when reopening About or Settings window
- Disconnected hooks for CLI-created sessions
